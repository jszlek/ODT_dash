# It is advisible to create conda environment with python == 3.8.12
# > conda create -n ODT_dash python=3.8.12
# then activate env
# > conda activate ODT_dash
# next install required pacakges
conda install -c conda-forge h2o=3.32.1.6 dash dash-html-compontents dash-core-compontents dash-table plotly pandas psutil
